services:
  - type: web
    name: job-application-assistant
    runtime: python
    buildCommand: uv sync && uv run playwright install chromium --with-deps
    startCommand: uv run python main.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
